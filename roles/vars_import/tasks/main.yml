---
- name: Import "{{vars_import}}"
  copy:
    src: "{{ lookup('file', 'packages/{{item}}') }}"
    dest: "{{vars_dest}}"
    owner: root
    group: root
    mode: '0777'
    backup: yes
  loop: "{{vars_import}}"
  tags: prerequisite
