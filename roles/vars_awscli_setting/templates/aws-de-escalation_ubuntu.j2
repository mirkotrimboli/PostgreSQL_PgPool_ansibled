#!/bin/bash

NETINT_ISPRESENT=`/usr/sbin/ip a | grep {{ sec_net_int }}`

while [ -n "$NETINT_ISPRESENT" ]
do
      /usr/sbin/ifconfig {{ sec_net_int }} down
      exit 0
done
/usr/sbin/ifconfig {{ sec_net_int }} down